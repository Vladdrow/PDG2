import { Router } from "express";
const router = Router();

router.get('/',);
router.get('/explore',);
router.get('/about',);
router.post('/register',);
router.post('/login',);

module.exports = router;